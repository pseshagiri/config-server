spring:
 application:
  name: employee-service
server:
 port: 8083

eureka:
 client:
  serviceUrl:   
   defaultZone: http://localhost:8085
  
logging:
  pattern: 
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"
    
springdoc:
  packagesToScan: com.seshagiri
